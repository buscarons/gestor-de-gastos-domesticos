import { ExpenseItem, IncomeItem } from './types';

// Pre-populating with empty arrays. 
// The app will now require user input or CSV import to initialize data.
export const INITIAL_DATA: ExpenseItem[] = [];

export const INITIAL_INCOME_DATA: IncomeItem[] = [];
